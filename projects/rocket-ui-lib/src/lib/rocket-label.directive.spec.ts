import { RocketLabelDirective } from './rocket-label.directive';

describe('RocketLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new RocketLabelDirective();
    expect(directive).toBeTruthy();
  });
});
