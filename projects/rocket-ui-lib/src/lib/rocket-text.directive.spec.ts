import { RocketTextDirective } from './rocket-text.directive';

describe('RocketTextDirective', () => {
  it('should create an instance', () => {
    const directive = new RocketTextDirective();
    expect(directive).toBeTruthy();
  });
});
